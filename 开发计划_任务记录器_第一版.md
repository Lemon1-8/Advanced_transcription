# 任务记录器 App 第一版 — 开发计划 (Flutter 原生)

> 基于 `任务记录器_App_第一版实现功能.md` 需求规格
> UI 设计参照 `任务记录器_ui_第一版参考稿.jsx`

---

## 一、技术栈

| 层面 | 选择 | 说明 |
|---|---|---|
| 框架 | Flutter (Dart) | 原生编译，一份代码产出 Android + iOS |
| 状态管理 | Provider + ChangeNotifier | 官方推荐，适合此规模项目 |
| 数据持久化 | Hive | 轻量 NoSQL 本地数据库，无需 SQL |
| 导航 | BottomNavigationBar + Navigator.push | 标准 Flutter 导航 |
| UI 风格 | Material 3 + 自定义 Theme | 还原原设计稿的 indigo 配色与圆角风格 |

---

## 二、项目结构 (32 个 Dart 文件)

```
bushuqi/
├── pubspec.yaml
├── lib/
│   ├── main.dart                    # 入口 + Hive 初始化 + Provider 配置
│   ├── app.dart                     # MaterialApp + Theme + 根导航壳
│   ├── utils/
│   │   ├── constants.dart           # 常量：状态映射、导航项、默认设置等
│   │   └── date_utils.dart          # 日期工具函数 (todayStr, isToday, formatDate 等)
│   ├── models/
│   │   ├── task.dart                # Task 数据模型 + JSON 序列化
│   │   ├── folder.dart              # Folder 数据模型 + JSON 序列化
│   │   └── app_settings.dart        # AppSettings 数据模型 + JSON 序列化
│   ├── providers/
│   │   └── app_provider.dart        # ChangeNotifier：所有 CRUD + 导航/弹窗状态
│   ├── widgets/
│   │   ├── status_mark.dart         # 状态标记 (□ / √ / √̶)
│   │   ├── task_card.dart           # 任务卡片
│   │   ├── section_title.dart       # 区域标题
│   │   ├── page_header.dart         # 页面顶部标题栏 (含返回按钮)
│   │   ├── settings_row.dart        # 设置行
│   │   ├── info_tile.dart            # 信息图块
│   │   ├── empty_state.dart         # 空状态占位
│   │   ├── filter_bar.dart          # 搜索页筛选栏 (分类/状态/日期)
│   │   ├── create_task_sheet.dart   # 新建/编辑任务底部弹窗
│   │   └── create_folder_sheet.dart # 新建/编辑文件夹底部弹窗
│   └── pages/
│       ├── home_page.dart           # 首页 (今日任务 + 统计卡片 + 搜索入口)
│       ├── category_page.dart       # 分类页 (文件夹列表 + 任务计数)
│       ├── category_task_page.dart  # 分类任务页
│       ├── date_page.dart           # 日期页 (今天/昨天/本月/更早)
│       ├── date_task_page.dart      # 日期任务页
│       ├── search_page.dart         # 查询页 (关键词 + 筛选 + 结果)
│       ├── stats_page.dart          # 统计页
│       ├── setting_page.dart        # 设置页
│       └── task_detail_page.dart    # 任务详情页
└── test/
    └── ...
```

---

## 三、数据模型

```dart
// Task
//   id: String          — UUID
//   title: String       — 标题（为空时从 description 取前 14 字）
//   description: String
//   folderId: String    — 引用 Folder.id
//   status: String      — 'todo' | 'done' | 'partial'
//   createdAt: String   — YYYY-MM-DD（本地时区）
//   updatedAt: String   — ISO 日期时间
//   notes: String

// Folder
//   id: String          — UUID
//   name: String        — 显示名称
//   description: String
//   createdAt: String   — ISO 日期时间

// AppSettings
//   sortOrder: String   — 'createdAt' | 'status'
//   deleteConfirm: bool — 默认 true
```

---

## 四、核心功能 (与需求规格完全一致)

| 功能 | 说明 |
|---|---|
| 新建任务 | 标题/描述/分类/备注，标题为空时取描述前 14 字 |
| 任务三状态 | 未完成 → 已完成 → 部分完成 → 未完成（循环切换） |
| 编辑任务 | 修改标题/描述/分类/备注，不可改创建日期 |
| 删除任务 | 确认弹窗，防止误删 |
| 文件夹分类 | 新建/重命名/删除文件夹，"未分类"不可删除 |
| 分类查看 | 按文件夹筛选任务列表 |
| 日期归档 | 系统自动按创建日期归档 |
| 日期查看 | 按今天/昨天/本月/更早分组查看 |
| 搜索筛选 | 关键词(标题/描述/备注/分类名) + 分类/状态/日期范围筛选 |
| 简单统计 | 今日完成率、总数/已完成/未完成/部分完成、本周/本月完成数 |
| 设置 | 默认首页、排序、删除确认开关、清空数据 |
| 本地存储 | Hive 持久化，无需登录，无需联网 |

---

## 五、状态管理

**`AppProvider` (ChangeNotifier)** 集中管理：

- **数据**：`tasks`, `folders`, `settings`
- **UI 状态**：`currentTab`, `pageParams`（子页面参数）、`overlay`（弹窗类型+数据）
- **方法**：`addTask`, `updateTask`, `deleteTask`, `toggleStatus`, `addFolder`, `renameFolder`, `deleteFolder`, `updateSettings`, `clearAllData`, `navigateToTab`, `navigateToPage`, `goBack`
- **持久化**：每次 mutation 后立即写回 Hive Box

---

## 六、导航方案

| 场景 | 实现方式 |
|---|---|
| 5 个主 Tab | BottomNavigationBar + IndexedStack（保持页面状态） |
| 子页面 (分类任务/日期任务/详情) | Navigator.push(MaterialPageRoute) |
| 搜索页 | Navigator.push |
| 新建/编辑弹窗 | showModalBottomSheet |
| 确认弹窗 | showDialog + AlertDialog |

---

## 七、UI 设计要点

- 配色：Indigo 主色调（复原参考稿的 indigo-500/600 体系）
- 首页渐变卡片：LinearGradient 实现紫蓝渐变
- 卡片圆角：28px / 30px / 32px 大圆角风格
- 底部导航：5 列，激活态 indigo 高亮
- FAB：右下角 indigo 圆形按钮
- 状态标记：三态圆形（空心/绿色实心/橙色实心+删除线）

---

## 八、实施阶段

### 阶段 1：环境搭建 & 项目脚手架 (≈30min)
1. 安装 Flutter SDK + Android Studio 配置
2. `flutter create bushuqi`
3. 配置 `pubspec.yaml`：添加 `provider`, `hive`, `hive_flutter`, `uuid`, `intl`
4. 创建目录结构

### 阶段 2：数据层 (≈1h)
1. 实现 `models/task.dart`, `models/folder.dart`, `models/app_settings.dart`
2. 实现 `utils/constants.dart`, `utils/date_utils.dart`
3. 实现 `main.dart` 中 Hive 初始化 + 数据加载

### 阶段 3：状态管理 (≈1.5h)
1. 实现 `providers/app_provider.dart`（所有 CRUD + 导航状态 + 持久化）
2. Provider 注入配置

### 阶段 4：UI 组件 (≈2h)
1. 基础组件（7 个）：StatusMark, TaskCard, SectionTitle, PageHeader, SettingsRow, InfoTile, EmptyState
2. 弹窗组件（2 个）：CreateTaskSheet, CreateFolderSheet
3. 筛选组件（1 个）：FilterBar

### 阶段 5：页面 (≈3h)
1. 主 Tab 页（5 个）：Home, Category, Date, Stats, Setting
2. 子页面（3 个）：CategoryTask, DateTask, TaskDetail
3. 搜索页（1 个）：Search

### 阶段 6：集成 (≈1.5h)
1. `app.dart`：MaterialApp + Theme + BottomNavigationBar + IndexedStack
2. 全局弹窗接入
3. Android 配置（App 名、主题色、状态栏）

### 阶段 7：验证 (≈1h)
1. `flutter run` 启动 Android 模拟器
2. 功能验收清单逐项测试
3. 边界情况测试（空数据、非法 ID、超长文本）
4. 数据持久化验证（杀掉进程重启）
5. `flutter build apk` 产出安装包

---

## 九、功能验收清单

- [ ] 新建任务 → 任务出现在今日列表
- [ ] 状态点击 → todo→done→partial→todo 循环
- [ ] 编辑任务 → 数据更新
- [ ] 删除任务 → 确认后消失
- [ ] 创建/重命名/删除文件夹
- [ ] 删除文件夹 → 内部任务移到"未分类"
- [ ] "未分类"不可删除不可重命名
- [ ] 分类页显示正确任务计数
- [ ] 日期页分组显示正确
- [ ] 关键词搜索 (标题/描述/备注/文件夹名)
- [ ] 筛选 (分类/状态/日期范围)
- [ ] 统计页数字正确（含完成率计算）
- [ ] 刷新/重启后数据保留
- [ ] 清空数据后重建默认文件夹

## 十、边界情况

- 标题空 + 描述空 → 显示"未命名任务"
- 标题空 + 描述有值 → 取描述前 14 字 + "..."
- 非法 taskId → 详情页显示"任务不存在"
- 非法 folderId → 重定向到分类页
- 无今日任务 → 首页空状态
- 唯一文件夹被删 → 全部归入未分类

---

## 十一、关键设计决策

1. **Provider (而非 BLoC/Riverpod)**：项目小，Provider 够用，学习成本最低
2. **Hive (而非 sqflite)**：NoSQL 轻量，JSON 序列化直观，无 SQL 依赖
3. **IndexedStack 保持 Tab 状态**：切换不丢失滚动/输入
4. **单 Provider 而非分散**：数据关联性强（任务↔文件夹），集中管理更简单
5. **手动 JSON**：不使用代码生成器，减少构建步骤
6. **先 Android 后 iOS**：Windows 开发环境，优先 Android 调试，iOS 需 Mac
