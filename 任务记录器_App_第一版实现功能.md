# 任务记录器 App 第一版实现功能

## 一、第一版目标

第一版的目标是做出一个**可正常使用的本地任务记录 App**。

核心原则：

- 功能简单
- 操作直观
- 数据本地保存
- 无需登录
- 先完成基础记录、分类、日期归档和查询功能
- 不加入过于复杂的扩展功能

第一版只需要解决一个核心问题：

> 用户可以方便地记录任务、标记任务状态，并能按分类、日期和条件查询任务。

---

## 二、第一版核心功能清单

第一版建议实现以下功能：

1. 新建任务
2. 查看任务列表
3. 修改任务状态
4. 编辑任务
5. 删除任务
6. 用户自定义文件夹分类
7. 按文件夹查看任务
8. 系统按日期自动归档
9. 按日期查看任务
10. 查询任务
11. 简单统计
12. 本地存储
13. 无需登录

---

## 三、任务管理功能

### 1. 新建任务

用户可以创建一条新的任务记录。

任务信息包括：

| 字段 | 是否必填 | 说明 |
|---|---|---|
| 任务标题 | 可选 | 任务的主要内容 |
| 任务描述 | 可选 | 对任务的补充说明 |
| 所属分类 | 可选 | 默认放入“未分类” |
| 任务状态 | 可选 | 默认为“未完成” |
| 创建日期 | 自动生成 | 系统自动记录 |
| 备注 | 可选 | 用于记录完成情况或其他说明 |

默认规则：

- 新建任务时，状态默认为“未完成”
- 如果用户没有选择分类，任务进入“未分类”
- 创建日期由系统自动记录，不需要用户手动填写
- 任务标题未填写时，默认为任务描述中前几个字

---

### 2. 查看任务列表

首页显示当天任务。

首页建议展示：

- 今日日期
- 今日任务数量
- 今日任务列表
- 快速新建任务按钮

任务列表中的每一条任务应显示：

| 内容 | 说明 |
|---|---|
| 状态图标 | 显示任务当前状态 |
| 任务标题 | 显示任务名称 |
| 所属分类 | 可选显示 |
| 创建时间 | 可选显示 |

示例：

```text
今天 · 2026年5月8日

□ 完成任务记录器需求分析
√ 整理 App 页面结构
√̶ 修改查询功能设计

＋ 新建任务
```

---

### 3. 修改任务状态

第一版需要支持三种任务状态：

| 状态 | 含义 |
|---|---|
| 空着 / □ | 未完成 |
| √ | 已完成 |
| √上有一横 / √̶ | 部分完成 / 已处理未完成 |

状态切换规则建议：

```text
未完成 → 已完成 → 部分完成 → 未完成
```

用户点击任务左侧状态图标即可切换状态。

---

### 4. 编辑任务

用户可以修改已创建的任务。

可编辑内容包括：

- 任务标题
- 任务描述
- 所属分类
- 任务状态
- 备注

不建议第一版允许用户修改创建日期，避免日期归档混乱。

---

### 5. 删除任务

用户可以删除不需要的任务。

删除前需要弹出确认提示：

```text
是否删除该任务？
删除后无法恢复。
```

---

## 四、分类功能

### 1. 用户自定义文件夹分类

用户可以自行创建文件夹分类。

示例分类：

- 学习
- 毕设
- 工作
- 运动
- 生活
- 未分类

第一版分类功能包括：

| 功能 | 是否实现 |
|---|---|
| 新建文件夹 | 是 |
| 重命名文件夹 | 是 |
| 删除文件夹 | 是 |
| 按文件夹查看任务 | 是 |
| 文件夹任务数量显示 | 是 |

---

### 2. 默认分类

系统需要提供一个默认分类：

```text
未分类
```

规则：

- 用户没有选择分类时，任务进入“未分类”
- 删除某个文件夹时，文件夹内任务移动到“未分类”
- “未分类”不能被删除

---

### 3. 分类页面

分类页面展示所有用户文件夹。

示例：

```text
分类

📁 学习        12个任务
📁 毕设        8个任务
📁 运动        5个任务
📁 生活        10个任务
📁 未分类      3个任务

＋ 新建文件夹
```

点击文件夹后进入该分类下的任务列表。

---

## 五、日期归档功能

### 1. 系统自动按日期分类

任务创建后，系统自动按照创建日期进行归档。

用户不需要手动创建日期分类。

日期分类示例：

```text
今天
昨天
2026年5月8日
2026年5月7日
2026年5月
2026年4月
```

---

### 2. 日期页面

日期页面用于按时间回顾任务。

示例：

```text
日期记录

今天
2026年5月8日    6个任务

昨天
2026年5月7日    4个任务

本月
2026年5月       23个任务

更早
2026年4月       41个任务
```

点击某一天或某个月后，展示对应日期范围内的任务列表。

---

## 六、查询功能

### 1. 查询入口

第一版建议在首页顶部提供搜索入口。

示例：

```text
🔍 搜索任务标题、描述、备注
```

用户点击后进入查询页面。

---

### 2. 查询页面

查询页面包含：

- 关键词输入框
- 分类筛选
- 状态筛选
- 日期筛选
- 查询结果列表

页面示例：

```text
查询任务

关键词：
[________________]

分类：全部分类 ▼
状态：全部状态 ▼
日期：全部时间 ▼

[搜索]

查询结果：
□ 完成毕业设计需求分析
√ 整理安卓 App 页面结构
√̶ 修改 E-R 图线条
```

---

### 3. 可查询内容

关键词搜索范围包括：

| 查询范围 | 说明 |
|---|---|
| 任务标题 | 主要查询内容 |
| 任务描述 | 辅助查询内容 |
| 备注 | 辅助查询内容 |
| 分类名称 | 可通过分类名查找任务 |

---

### 4. 筛选条件

第一版建议支持以下筛选条件。

#### 按分类筛选

```text
全部分类
学习
毕设
工作
运动
生活
未分类
```

#### 按状态筛选

```text
全部状态
未完成
已完成
部分完成
```

#### 按日期筛选

```text
全部时间
今天
昨天
近7天
本周
本月
自定义日期
```

---

### 5. 查询结果展示

查询结果应使用任务列表形式展示。

每条结果显示：

| 内容 | 是否显示 |
|---|---|
| 状态图标 | 必须 |
| 任务标题 | 必须 |
| 所属分类 | 建议显示 |
| 创建日期 | 建议显示 |
| 简短描述 | 可选 |

示例：

```text
查询结果：共 12 条

□ 完成毕业设计需求分析
   分类：毕设｜2026年5月8日

√ 整理安卓 App 页面结构
   分类：学习｜2026年5月7日

√̶ 修改 E-R 图线条
   分类：毕设｜2026年5月6日
```

---

### 6. 查询结果操作

用户在查询结果页中可以直接操作任务：

- 点击状态图标：切换任务状态
- 点击任务内容：进入任务详情
- 长按任务：编辑、删除、移动分类

---

## 七、统计功能

第一版只做简单统计即可。

统计页面建议包含：

| 统计项 | 说明 |
|---|---|
| 今日任务数 | 今天创建的任务总数 |
| 今日已完成数 | 今天状态为“已完成”的任务数量 |
| 今日未完成数 | 今天状态为“未完成”的任务数量 |
| 今日部分完成数 | 今天状态为“部分完成”的任务数量 |
| 本周完成数 | 本周完成任务数量 |
| 本月完成数 | 本月完成任务数量 |

示例：

```text
今日统计

总任务：8
已完成：4
未完成：3
部分完成：1

完成率：50%
```

完成率计算规则：

```text
完成率 = 已完成任务数 ÷ 总任务数 × 100%
```

其中，“部分完成”不计入已完成。

---

## 八、设置功能

第一版设置页面保持简单。

建议包含：

```text
设置

数据存储：仅本机保存
默认首页：今日任务
任务排序：创建时间 / 完成状态 / 手动排序
删除确认：开启 / 关闭
清空全部数据
关于任务记录器
```

第一版必须说明：

> 所有数据仅保存在本机，不需要登录账号。

---

## 九、本地存储功能

第一版必须支持本地存储。

本地存储要求：

- 任务数据保存在手机本地
- 分类数据保存在手机本地
- 查询历史可以选择保存或不保存
- 卸载 App 后数据可能被清除
- 不需要账号
- 不需要联网

建议在设置页显示说明：

```text
当前数据仅保存在本机。
如果卸载 App，数据可能会被删除。
```

---

## 十、页面结构

第一版建议包含以下页面：

| 页面 | 功能 |
|---|---|
| 首页 | 查看今日任务、快速新建任务 |
| 新建任务页 | 创建任务 |
| 任务详情页 | 查看任务详情 |
| 编辑任务页 | 修改任务内容 |
| 分类页 | 查看和管理文件夹分类 |
| 分类任务页 | 查看某个分类下的任务 |
| 日期页 | 按日期查看任务 |
| 日期任务页 | 查看某一天或某个月的任务 |
| 查询页 | 搜索和筛选任务 |
| 统计页 | 查看简单完成情况 |
| 设置页 | 管理基础设置 |

---

## 十一、底部导航设计

第一版推荐使用五个底部导航入口：

```text
首页｜分类｜日期｜统计｜设置
```

查询功能放在首页顶部搜索框中，不单独占用底部导航。

原因：

- 查询是辅助功能，不一定每天都用
- 首页搜索入口更直观
- 底部导航保持简洁

---

## 十二、第一版不做的功能

为了降低开发难度，第一版不建议实现以下功能：

```text
账号登录
云同步
多人协作
好友功能
提醒通知
复杂日历视图
标签系统
任务优先级
语音输入
图片附件
文件附件
AI 自动分类
数据导出
数据备份
跨设备同步
```

这些功能可以作为后续版本扩展。

---

## 十三、第一版完整使用流程

### 1. 创建任务流程

```text
打开 App
↓
点击“＋ 新建任务”
↓
输入任务标题
↓
选择分类
↓
保存任务
↓
任务出现在首页今日列表中
```

---

### 2. 修改任务状态流程

```text
打开首页
↓
找到任务
↓
点击任务左侧状态图标
↓
状态在“未完成 / 已完成 / 部分完成”之间切换
```

---

### 3. 按分类查看任务流程

```text
进入分类页
↓
选择一个文件夹
↓
查看该分类下的所有任务
```

---

### 4. 按日期查看任务流程

```text
进入日期页
↓
选择某一天或某个月
↓
查看该时间范围内的任务
```

---

### 5. 查询任务流程

```text
点击首页搜索框
↓
输入关键词或选择筛选条件
↓
点击搜索
↓
查看查询结果
↓
点击任务进入详情或直接修改状态
```

---

## 十四、第一版优先级划分

### 必须实现

| 功能 | 优先级 |
|---|---|
| 新建任务 | 高 |
| 查看任务列表 | 高 |
| 修改任务状态 | 高 |
| 编辑任务 | 高 |
| 删除任务 | 高 |
| 本地存储 | 高 |
| 用户自定义分类 | 高 |
| 日期自动归档 | 高 |
| 查询任务 | 高 |

---

### 建议实现

| 功能 | 优先级 |
|---|---|
| 简单统计 | 中 |
| 文件夹重命名 | 中 |
| 文件夹任务数量显示 | 中 |
| 自定义日期查询 | 中 |
| 删除确认提示 | 中 |

---

### 可以延后

| 功能 | 优先级 |
|---|---|
| 查询历史 | 低 |
| 手动排序 | 低 |
| 复杂统计图表 | 低 |
| 数据导出 | 低 |
| 数据备份 | 低 |

---

## 十五、第一版总结

第一版的核心功能可以概括为：

```text
任务记录
三状态标记
文件夹分类
日期归档
条件查询
本地保存
无需登录
```

第一版完成后，App 应该可以满足用户的基本使用需求：

- 记录今天要做的事情
- 标记任务是否完成
- 把任务放入不同分类
- 按日期回顾以前的任务
- 通过关键词和筛选条件查找任务
- 在本机安全保存数据

这就是“任务记录器”第一版最适合实现的功能范围。
